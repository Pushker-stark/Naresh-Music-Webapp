{
    "builds": [{
        "src": "FavoriteTunes/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "FavoriteTunes/wsgi.py"
        }
    ]
}